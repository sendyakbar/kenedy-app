import { FC } from "react";
import { Props } from "./types";
import { Text, View } from "react-native";

export const JobMatchesScreen: FC<Props> = () => {
    return (
        <View>
            <Text>
                Hello this is Job Matches Screen
            </Text>
        </View>
    )
}